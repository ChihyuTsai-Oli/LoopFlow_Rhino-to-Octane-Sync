# LoopFlow R2O 2.0 — 實作總覽

本文件是 2.0 實作範圍、架構邊界與完成關卡的唯一來源。詳見同目錄 `資料契約.md`、`工作流程.md`、`開發任務與路徑.md`、`系統設定.md`、`重構進度.md`。

## 目前狀態

- 目標版本：`v2.0.0`（尚未發布）
- 穩定參考：`v1.0.0`／`main`（1.x）
- Runtime：Rhino 8／CPython 3.9；OctaneRender Standalone **2026.4**／Lua（2.0 開發／驗收目標）
- 開發模式：程式與重構文件在 `wip/`；`main` 維持 1.x
- 目前階段：**正式文件骨架＋發布契約已寫入**；執行時根與 R2B 同型。Octane 測試 Lua 空殼在測檔根 `_LoopFlow_Config/loopflow_R2O/lua/`。尚未：功能碼、正式 yak、公開 USER_GUIDE 改寫
- Package Manager 畫面名已凍結：`loopflow Rhino to OctaneRender Sync`；yak **只含 Rhino**，Octane Lua 留 GitHub
- 跨產品順序：**先 R2B＋Blender（已穩），再本產品**
- 開發切片：**Rhino producer 與 Octane Lua consumer 同一功能同批交付**
- **第一條垂直功能：相機**（B 骨架之後）；該批須實測中文工作檔路徑。通過後再 Point／Models／Scatter

## 實作文件閱讀順序

1. `實作總覽.md`
2. `資料契約.md`
3. `工作流程.md`
4. `開發任務與路徑.md`
5. `系統設定.md`
6. `重構進度.md`

`rhino指令.md`：開發按鈕。`前期規劃/`：原則與決策過程；與六份衝突時以六份為準。

## 產品邊界

```text
Rhino Producer（Models／Camera／Point／Scatter／Open）
        ↓ 安全發布
Octane LiveLink Consumer（Camera／Point；Models＝手動置換）
Octane Authoring Tools（獨立；2.0 不擴充功能）
```

## 重構模式

乾淨重建、一次切換；無長期 v1 alias／雙寫。升級靠獨立 migration。隔離 scripts、shortcut、scene、輸出。

## 完成關卡（摘要）

1. 六份正式文件骨架（已完成）。
2. A01 盤點加厚契約與工作流程可測表。
3. B 骨架 → **相機兩端（含中文路徑實測）** → Point／Models／Scatter → 端到端＋migration／**Rhino `.yak`（Octane Lua 留 GitHub）**／RC → 合入 `main` 發 `v2.0.0`（該 tag 永不移動）。
