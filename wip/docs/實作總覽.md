# LoopFlow R2O 2.0 — 實作總覽

本文件是 2.0 實作範圍、架構邊界與完成關卡的唯一來源。詳見同目錄 `資料契約.md`、`工作流程.md`、`開發任務與路徑.md`、`系統設定.md`、`重構進度.md`。

## 目前狀態

- 目標版本：`v2.0.0`（尚未發布）
- 穩定參考：`v1.0.0`／`main`（1.x）
- Runtime：Rhino 8／CPython 3.9；**OctaneRender Studio+ 2026.4**／Lua（2.0 開發／驗收目標）
- 開發模式：程式與重構文件在 `wip/`；`main` 維持 1.x
- 目前階段：**相機已通過**；**點位已通過**。**Models 已合入 `v2-development`**。**Objects 本批**（`R2O_Objects` → `models/R2O_Objects_時戳.usdz`；Octane 自行載入）。尚未：完整 Open／Health、正式 yak、公開 USER_GUIDE 改寫；`main` 仍為 1.x
- Package Manager 畫面名已凍結：`loopflow Rhino to OctaneRender Sync`；yak **只含 Rhino**，Octane Lua 留 GitHub
- 跨產品順序：**先 R2B＋Blender（已穩），再本產品**
- 開發切片：**Rhino producer 與 Octane Lua consumer 同一功能同批交付**
- **第一條垂直功能：相機**（已通過）。**第二條：點位**（已通過）。**第三條：Models**（已合入）。**第四條：Objects**（本批；無 LiveLink）。不標整條產品 Golden。

## 實作文件閱讀順序

1. `實作總覽.md`
2. `資料契約.md`
3. `工作流程.md`
4. `開發任務與路徑.md`
5. `系統設定.md`
6. `重構進度.md`

`rhino指令.md`：開發按鈕。`前期規劃/`：原則與決策過程；與六份衝突時以六份為準。

## 產品邊界

```text
Rhino Producer（Models／Camera／Point／Scatter／Open）
        ↓ 安全發布
Octane LiveLink Consumer（Camera／Point；Models＝關再開重讀 USDZ）
Octane Authoring Tools（獨立；2.0 不擴充功能）
```

## 重構模式

乾淨重建、一次切換；無長期 v1 alias／雙寫。升級靠獨立 migration。隔離 scripts、shortcut、scene、輸出。

## 完成關卡（摘要）

1. 六份正式文件骨架（已完成）。
2. A01 盤點加厚契約與工作流程可測表。
3. B 骨架 → 相機（已通過）→ 點位（已通過）→ Models（已合入）→ **Objects（本批）** → 端到端＋migration／**Rhino `.yak`（Octane Lua 留 GitHub）**／RC → 合入 `main` 發 `v2.0.0`（該 tag 永不移動）。
