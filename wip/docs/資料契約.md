# LoopFlow R2O 2.0 — 資料契約

本文件是 2.0 指令、Python↔Lua schema、Octane node 與跨軟體資料契約的權威來源。欄位級細節待盤點加厚；已決原則如下。衝突先改 `前期規劃/資料生態決策表_三家建議.md` 再回寫。

## 契約狀態

- 套用版本：R2O `v2.0.0`
- 舊版參考：`v1.0.0`
- 階段：**相機通道已凍結**；PM 畫面名已凍結；執行時根與 R2B 同型。Point／Models／Scatter 檔名與 schema 尚未凍結
- 開發期指令：`rhino指令.md`

## 核心裁決（已確認）

| 主題 | 決定 |
|---|---|
| 設定／即時檔根 | 與 R2B 同型：已存檔 `.3dm` 旁 `_LoopFlow_Config/loopflow_R2O/`；不用 `exchange/`、不用 AppData／ProgramData 當交換根。中文路徑待相機實機確認 |
| Consumer 路徑（XF-ED-04） | **C**：指標＋手動覆寫 |
| Models 消費（ED-02） | **A**：Octane **僅手動置換** USDZ；不做 Models LiveLink |
| Point 身分（ED-05） | 完整圖層路徑映射之穩定 `type_id`；不寫 GUID／UserText 回 Rhino |
| 刪點（ED-10） | **A**：權威 snapshot 成功後刪**受管**節點＋報告 |
| Camera 發布（ED-13） | Rhino event＋throttle；補 trailing／手動 |
| 資料格式（ED-18） | **B**：JSON；Lua 只 parse／apply，禁止對專案 `loadfile()` |
| Camera 消費（ED-19） | **B**：有 TIMER 才 real-time；否則跑腳本套用一次。禁止主執行緒空轉輪詢 |
| Scatter（ED-01） | 維持獨立通道 |
| Scatter Block 定義慣例 | 建 Block **前**先擺正（建議朝 ±X／±Y）；插入點／局部原點在世界 `(0,0,0)`；與其他 USD 差直角時在**定義內**旋轉對齊，使 Octane Scatter 旋轉軸一致（詳見 `工作流程.md`） |
| Scatter 匯出還原 | 匯出可暫移；結束後來源 Block **不得**留在原點 |
| 操作序 | Models → Camera → Point → Scatter |
| 舊版升級（XF-ED-02） | 不升級混用 |
| 開發順序 | R2B 主鏈已穩；本產品 **相機兩端本批**，再 Point／Models／Scatter |
| 相機指令 | **已凍結**：`R2O_Camera`（開／關持續發布）、`R2O_Camera_Push`（手動一次） |
| 相機檔 | **已凍結**：`live/camera.json`（pending 同層）；log `r2o.log` |
| 本機指標 | **已凍結**：`%APPDATA%\LoopFlow\R2O\current_project.json`（XF-ED-04；不放 live） |
| 上架 | Rhino **只**打 `.yak`；Octane Lua 留 GitHub；PM 畫面名 `loopflow Rhino to OctaneRender Sync` |

## 共通規則

- Python 與 Lua 共用 versioned schema／fixtures。
- **軟體介面一律英文**（Rhino 對話框／彈窗、Octane 腳本提示）。文件與程式註解仍用繁中。
- Consumer 僅 parse＋apply 成功才更新狀態。
- pending→validate→atomic；last-good 保護。
- 來源 `.3dm` 狀態還原；Scatter 不留來源 Block 位移。
- Point 身分以完整路徑 `type_id` 為準（**不再**要求「終端圖層名全域唯一」；那是 1.x 舊規則）。
- 資產用 Block 建議與 Point 掃描圖層分開（例如 `USD::`）。

執行時路徑與 R2B 相同：設定、相機／點位 JSON、USDZ 都在工作檔旁 `_LoopFlow_Config/loopflow_R2O/`。家中已測 USD 進 Octane、Lua 路徑可含中文；**相機實機通過前不另開英文交換根**。若相機實測失敗再改契約。

AppData 只放 XF-ED-04「目前專案指標」（這台電腦正在做哪案），不放 live／models。

### 相機 JSON（schema_version = 1）

欄位對齊 1.x Lua：`position`／`target`／`up_vector` 為公尺 Y-up `[x,y,z]`（Rhino `(x,z,-y)`）；`fov_degrees` 由 35mm 等效鏡頭算出，感光寬 **36mm**，鏡頭 ≤ 0 退回 50mm。另含 `producer`、`document`、遞增 `revision`。Lua **只 parse JSON**，禁止 `loadfile()` 同步檔。

Octane 目標：**恰好一台**已從 Render Target Expand 的 Thin Lens。零台或多台停止並英文說明，不猜第一台。

Camera 消費（ED-19）：**禁止 Sleep／busy-loop**（會凍結 Octane）。若 API 有 TIMER 才開 real-time 視窗；否則 Ctrl+Q／跑腳本＝套用目前 `camera.json` 一次（與 1.x 相同）。明顯 lag 不刪開關，維持可關。

Lua 讀中文工作檔路徑：指標帶 Windows 8.3 `config_root_short`；再不行才用 UTF-16 開檔。不要用會對 UTF-8 路徑失敗的 `io.open` 當唯一手段。

Package Manager 畫面名：**`loopflow Rhino to OctaneRender Sync`**（已決；官方拼法含 e）。yak 只含 Rhino；Octane Lua／Authoring 不進包。

公開使用說明改寫等 **本產品程式完成後再與 R2B 一起寫**。

## 必須盤點（後續）

Point／Models／Scatter 的 command、USD／USDZ／JSON 檔名、`type_id` 對照表、Octane node／group／shortcut。相機指令與 `camera.json` 已凍結。
