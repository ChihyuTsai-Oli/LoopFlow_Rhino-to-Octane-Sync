# R2O 2.0 — 資料生態決策表（三家建議）（合併建議）

> 由 `資料生態決策表_三家建議.md` 產生（2026-08-27）。三欄 AI 建議已併為「整合建議」；「你的決定」照原表。權威來源仍是原表；本檔僅供閱讀。詳見同目錄 `操作流程模擬.md`。


本檔為閱讀用合併稿。裁決請改原檔 `資料生態決策表_三家建議.md`，再重跑 `merge_decision_table.py`。

## 使用方式

- 直接改「你的決定／說明」，或在對話回覆 ID（例如 `R2O-ED-03 採用 A`）。
- 請保留 `XF-*`／`R2O-*` 編號。
- ND 若標「由某 ED 統一裁決」，只填上方對應列。
- **確認前不寫正式產品功能碼**。跨產品 `XF-*` 須與 R2B 決策表同文同日。

### 整合建議怎麼讀

- 一欄濃縮 Grok／Claude／Codex；標明強度票數與方向。
- 以最右欄「你的決定」為準；衝突時以決定欄覆寫建議方向。

### AI 建議強度

| 強度 | 意義 |
|---|---|
| **強烈建議** | 關係資料不斷鏈、可復原、避免靜默錯誤 |
| **一般建議** | 可改，但有維護／操作代價 |
| **輕鬆建議** | 不影響核心鏈 |

---

## A. 跨產品原則（與 R2B 同文確認一次）

| ID | 建議原則 | 現況／選項 | 整合建議（Grok／Claude／Codex） | 你的決定／說明 |
| --- | --- | --- | --- | --- |
| XF-ECO-01 | 工作檔未存檔：會寫設定／發布的指令一律停止 | A 全擋；B 僅寫檔類擋 | **共識 強烈建議×3**（G強／C強／X強）。方向：採 B。B；現況 R2O Camera 的說明明寫「不需先存檔，新檔也能立刻同步」（`LiveLink_R2O_Camera.py:14`），因為它寫進 AppData。 | **自動採用（強烈建議×3，2026-08-27）**：採 B；採用「未存檔即停」 |
| XF-ECO-02 | `_LoopFlow_Config/<product>/`；不用 `exchange/` 當執行時根 | 已定父路徑 | **共識 強烈建議×3**（G強／C強／X強）。維持；(1) Octane 找不到路：Octane 端 Lua 是寫死 `…` 去讀設定（`LiveLink_R2O_Camera.lua:24-27`、`LiveLink_R2O_Point.lua:25-28`），而 Octane 完全不知道 Rhino 現在開的是哪個 `.3dm`。 | **已採用**（2026-08-26）：父路徑；見 E 區 |
| XF-ECO-03 | 來源 Rhino 文件零變更；失敗保留 last-good | 已寫 | **共識 強烈建議×3**（G強／C強／X強）。採用；計畫已列 `MaterialSource`…`_-Save`（`LiveLink_R2O_Models.py:184`…`LiveLink_R2O_Scatter.py:103、126`），而搬回不在 `try/finally`…`doc.Modified` 必須與執行前相同。 | **已採用** |
| XF-ECO-04 | 暫存 → pending → validate → atomic last-good | 已寫 | **共識 強烈建議×3**（G強／C強／X強）。採用；Models 與 Scatter 都是「先 `os.remove` 舊檔、再匯出新檔」（`LiveLink_R2O_Models.py:340-344`、`LiveLink_R2O_Scatter.py:109-113`…`safe_write_text_atomic()`（`LiveLink_R2O__Config… | **已採用** |
| XF-ECO-05 | Consumer 僅 parse＋apply 成功才更新狀態 | 已寫 | **共識 強烈建議×3**（G強／C強／X強）。採用；Octane 兩支 Lua 在 parse 失敗時都會直接 return、不動節點（`LiveLink_R2O_Point.lua:102-112`…`LiveLink_R2O_Camera.lua:72-75`），這是套用失敗，狀態不該算成已同步。 | **已採用** |
| XF-ECO-06 | 不與 R2B／LoopFlow 共用 runtime package | 已定 | **共識 強烈建議×2**（G強／C一／X強）。採用；建議兩產品各自實作，卻共用同一份行為矩陣與 fixtures：atomic publish、節流、結果狀態、錯誤 stage、last-good／pending 語意，避免規格再次漂移。 | **已採用** |
| XF-ECO-07 | 乾淨重建、一次切換；核心不雙寫 | 已定 | **共識 強烈建議×3**（G強／C強／X強）。採用；補一點好消息：XF-ECO-02 把執行時路徑從 AppData 改到工作檔旁，本身就讓 1.x 與 2.0 指向不同位置，隔離是自動達成的，不需要額外的 alias 或版本旗標。 | **已採用** |
| XF-ED-01 | 本機偏好／語系 | A 不做；B AppData；C 寫專案 | **共識 輕鬆建議×2**（G一／C輕／X輕）。方向：採 A。現況兩個產品都沒有語系設定，也沒有實際需求，先不做的成本是零。若日後要做，只能是 B：語系是「這台電腦前面這個人」的偏好，不是專案屬性，寫進專案設定會在公司與家中兩台機器之間互相蓋掉。；本機偏好可留 AppData，專案設定只保存會影響團隊重現結果的資料，避免語系在雙機之間互相覆蓋。 | A，全英文即可 |
| XF-ED-02 | 舊版升級 | A migration 必做；B 重裝即可 | **共識 一般建議×3**（G一／C一／X一）。方向：採 B。設定可 B；scene／shortcut 視改名再定；R2O 設定只有 9 個純文字欄位（`LiveLink_R2O__Config.py:45-55`），重填成本很低；但 `PointLayer`、`LastModelLayer`、`ModelDir`…`R2O_Path.txt`…`R2O-ND-06`／`ND-11` 決定。 | 不升級，新舊版不可混用 |
| XF-ED-03 | 契約未定前可否建空殼 `wip/src`／entrypoints | A 不建；B 可空殼並行 | **共識 一般建議×3**（G一／C一／X一）。方向：採 B。B（配合 `rhino指令.md` 建鈕）；`rhino指令.md` 已經寫好 5 顆按鈕的巨集、並註明入口尚未建立；現在按下去只會噴 Rhino 的檔案不存在錯誤。空殼入口先回一句明確訊息（例如「R2O_Models：2.0 尚未實作」），測試按鈕才有意義，也能順便驗證巨集路徑是對的。 | B |
| XF-ND-01 | 兩邊 config 資料夾檔名同型規則 | A 同型＋產品前綴；B 各自命名 | **共識 一般建議×3**（G一／C一／X一）。方向：採 A。A；同型的重點不是檔名好看，而是兩個產品失敗後長得一樣，你才不必記兩套。建議規則：`config.json`、`<channel>_pending.<ext>`、`<channel>.<ext>`（last-good）、`<product>.log`； | A |
| XF-ED-04 | ECO-02 之後，consumer 如何得知目前專案的 `_LoopFlow_Config/loopflow_R2x/` 位置 | A Rhino 在 AppData 寫一個「目前專案指標檔」，consumer 讀指標；B consumer 端手動填路徑；C 兩者都有（指標為預設，可手動覆寫） | **共識 強烈建議×2**（G缺／C強／X強）。方向：採 C。現況兩邊 consumer 都寫死 AppData：Octane Lua 直接組 `%APPDATA%\...\LoopFlow_R2O\Data`（`LiveLink_R2O_Point.lua:25-28`…`LoopFlow_import_3dm/__init__.py:550-555`…`rhino_json…；手動覆寫優先於本機指標；指標至少含 product、正規化絕對路徑、來源文件識別、更新時間與版本。路徑不存在、product 不符或指標過舊時必須停止並提示，不可猜最近資料夾。 | C |

---

## B. R2O 上位原則

| ID | 建議原則 | 現況／選項 | 整合建議（Grok／Claude／Codex） | 你的決定／說明 |
| --- | --- | --- | --- | --- |
| R2O-ECO-01 | 三邊界：Rhino Producer｜Octane LiveLink｜Authoring（本輪不擴充） | 已寫 | **共識 強烈建議×3**（G強／C強／X強）。採用；我把 8 支 Lua 用到的 `octane.*` 全數盤過：LiveLink 兩支只用 8 個常數且完全沒有碰 `octane.gui`；Authoring 四支用另外 15 個常數，而且都依賴 `octane.gui`。 | **已採用** |
| R2O-ECO-02 | Octane 端維持 Lua；Rhino Python-first | 已寫 | **共識 強烈建議×3**（G強／C強／X強）。採用；Lua 端沒有 JSON parser，所以現行做法是把資料檔寫成可執行的 Lua，consumer 用 `loadfile()` ＋ `pcall()` 執行它（`LiveLink_R2O_Point.lua:102-108`…`R2O-ND-05` 因此不是整潔問題，是安全問題。 | **已採用** |
| R2O-ECO-03 | Models 材質後處理失敗＝整次 Models 失敗 | 已寫 | **共識 強烈建議×3**（G強／C強／X強）。採用；`_promote_material_bindings()` 整段用 `except Exception: pass` 吞掉所有錯誤（`LiveLink_R2O_Models.py:159-160`），外層只檢查「檔案存在」就印出成功（:362-364）。 | **自動採用（強烈建議×3，2026-08-27）**：採用 |
| R2O-ECO-04 | Models／Scatter／Point／Camera 可獨立執行（無強制順序） | 1.x 傾向獨立 | **共識 一般建議×3**（G一／C一／X一）。維持獨立；文件可寫建議順序；Point 送出的是「type ＋ 變換矩陣」，type 取自末層圖層名； | **自動採用（一般建議×3，2026-08-27）**：維持獨立 |
| R2O-ECO-05 | 圖層末段名稱不是唯一永久 ID；穩定 ID 另定 | 待盤點 | **共識 強烈建議×3**（G強／C強／X強）。採用；欄位見 ND；現況末層名同時扮演三個角色：Octane 節點名、分組鍵、唯一身分（`LiveLink_R2O_Point.py:73`）。 | **自動採用（強烈建議×3，2026-08-27）**：採用 |

---

## C. R2O 需確認（工作流／產品語意）

| ID | 需要確認 | 現況觀察／選項 | 整合建議（Grok／Claude／Codex） | 你的決定／說明 |
| --- | --- | --- | --- | --- |
| R2O-ED-01 | Scatter 是否維持獨立發布通道 | A 獨立 Scatter；B 併入 Models＋Proxy | **共識 一般建議×3**（G一／C一／X一）。方向：採 A。A（與 1.x 一致）；但 A 有一個代價必須寫進使用說明：Scatter 的輸出檔名是 Block 名經 `normalize_type_name()` 轉換而來（`LiveLink_R2O_Scatter.py:106`），Point 的 type 卻是完全未轉換的圖層名（`LiveLink_R2O_Point.py:93`）。 | **自動採用（一般建議×3，2026-08-27）**：採 A；A（與 1 |
| R2O-ED-02 | Models 在 Octane 的消費方式 | A 手動置換 USDZ；B LiveLink 腳本；C 兩者都要 | **共識 一般建議×3**（G一／C一／X一）。方向：採 C。C，文件寫清何時用哪個；現況只有 A 存在：USDZ 後處理刻意把材質綁定提升到 `/Rhino/Geometry/<LayerName>` 這個 Xform，目的就是讓手動置換 USDZ 時材質不會掉（`LiveLink_R2O_Models.py:40-51` 的說明）。。選項字母有差（C／C／A），以「你的決定」為準 | **採用 A（2026-08-27）**：Octane Models **僅手動置換** USDZ。依實測 Octane 端只能手動；2.0 不做 Models LiveLink。 |
| R2O-ED-03 | 取消／失敗／中斷的使用者可見回饋 | 靜默／命令列／彈窗＋stage | **共識 一般建議×2**（G一／C一／X強）。彈窗簡述＋可開 log；6 支 Python 裡只有 Scatter 會跳 MessageBox（`LiveLink_R2O_Scatter.py:60-65`），其餘全部只 `print` 到指令列； | **自動採用（一般建議×2，2026-08-27）**：彈窗簡述＋可開 log |
| R2O-ED-04 | Authoring 與 LiveLink 安裝關係 | A 同包；B 可選／分開 ZIP；C 2.0 不帶 Authoring | **共識 輕鬆建議×2**（G一／C輕／X輕）。方向：採 B。依據是 ECO-01 的盤點結果：Authoring 依賴 `octane.gui`，LiveLink 完全不依賴。分開裝可以讓 LiveLink 的 Octane 版本相容矩陣不被 Authoring 的 GUI API 綁住——Octane 改版時最容易壞的就是 GUI 那一層。；Authoring 與 LiveLink 分開安裝、各自宣告 Octane 相容版本，但可在同一 release manifest 列出已驗證組合；移除 Authoring 不得改動 LiveLink 設定或節點。。選項字母有差（A／B／B），以「你的決定」為準 | **自動採用（輕鬆建議×2，2026-08-27）**：採 B；依據是 ECO-01 的盤點結果：Authoring 依賴 `octane.gui`，LiveLink 完全不依賴 |
| R2O-ED-05 | Point 穩定身分 | A Block 定義名；B instance id；C 新 GUID 由 R2O 寫入；D 完整圖層路徑 | **共識 強烈建議×3**（G強／C強／X強）。方向：採 C。要有穩定 ID；不要只靠末層同名；(1) 群組（＝Octane 節點）確實需要穩定 ID，因為節點名就是它的身分（`LiveLink_R2O_Point.lua:150`）：建議用完整圖層路徑正規化後的 `type_id`，另外在 payload 存 `display_name` 與 `source_layer_path`。 | **採用（2026-08-27）**：群組身分＝完整圖層路徑映射之穩定 `type_id`（專案設定外部對照）；另存 `display_name`／`source_layer_path`；**不寫任何 GUID／UserText 回 Rhino**（不採 C）。單點可帶 Rhino GUID 供診斷，不作 Octane apply 對齊鍵。 |
| R2O-ED-06 | 同末層名、不同父圖層 | A 一個 Octane 節點；B 多個；C 報錯 | **共識 強烈建議×2**（G一／C強／X強）。方向：採 B。現況是靜默合併，且已寫在程式說明裡（`LiveLink_R2O_Point.py` 說明第 23-24 行）。若 `type_id` 改用完整路徑，同末層名自然產生兩個節點；；顯示名稱可以同名，節點技術鍵不得同名；正規化後若仍碰撞，整批在 apply 前報錯，不可到一半才發現。 | **自動採用（強烈建議×2，2026-08-27）**：採 B；現況是靜默合併，且已寫在程式說明裡（`LiveLink_R2O_Point.py` 說明第 23-24 行） |
| R2O-ED-07 | Scatter 單項失敗 | A 略過該項繼續；B 整批中止 | **共識 一般建議×2**（G一／C一／X強）。方向：採 A。A＋摘要列出失敗項；程式已經收集 `failed_names` 並在最後列出（`…`）。真正的缺口是：失敗那一項的 Block 可能已經被搬到原點而沒搬回去（`:103` 之後若丟例外就跳出整個迴圈）。 | **自動採用（一般建議×2，2026-08-27）**：採 A；A＋摘要列出失敗項 |
| R2O-ED-08 | Octane 缺少對應 Proxy | A 建空；B 略過；C 失敗並說明 | **共識 一般建議×3**（G一／C一／X一）。方向：採 B。C 或 B＋明確訊息，不要靜默；現況是 A：sync 檔有某個 type，Lua 就建一個節點，節點裡沒有幾何——不會失敗，只是空的。改成 C（整批失敗）會讓「只是還沒匯出某一種家具」變成整次同步做不了，日常工作太硬。。選項字母有差（C／B／B），以「你的決定」為準 | **自動採用（一般建議×3，2026-08-27）**：採 B；C 或 B＋明確訊息，不要靜默 |
| R2O-ED-09 | 節點名稱碰撞 | A 改名；B 覆寫；C 略過；D 報錯 | **共識 強烈建議×2**（G一／C強／X強）。(1) Scatter 檔名碰撞：`normalize_type_name()`…`unnamed_block`（`LiveLink_R2O__Config.py:149-152`）——兩個中文名 Block 會同時寫成 `unnamed_block.usd`，後者直接蓋掉前者，沒有任何訊息。；producer 在任何寫檔前先計算全部檔名／`type_id`，consumer 在任何 apply 前先檢查全部節點鍵；只要碰撞就整批停止，並列出兩個原始來源，禁止自動覆寫或臨時加尾碼。 | **自動採用（強烈建議×2，2026-08-27）**：(1) **Scatter 檔名碰撞**：`normalize_type_name()` 會把非 ASCII 字元全部換成底線再 strip 掉，所以純中… |
| R2O-ED-10 | Rhino 已刪的 type／點 | A 刪 Octane 節點；B 孤立留下；C 標 inactive | **共識 一般建議×2**（G一／C一／X強）。方向：採 A。C 或 A＋可還原策略寫進契約；現況已經是 A，只是更新與清理用了不同範圍：更新是全場景搜尋（`LiveLink_R2O_Point.lua:137`），清理只清 ng 群組內的節點（`:182-187`）。。選項字母有差（C／A／A），以「你的決定」為準 | **採用 A（2026-08-27）**：權威 snapshot 成功套用後，刪除 LiveLink **受管**節點並列入報告；不採 inactive；清理範圍與更新範圍一致（僅管理群組），不掃使用者節點。 |
| R2O-ED-11 | 未知 type | A 佔位；B 忽略；C 失敗 | **共識 一般建議×3**（G一／C一／X一）。方向：採 B。B＋報告，或 C；現行 Lua 有 `…`（`LiveLink_R2O_Point.lua:117`），意思是缺 type 的項目會被靜默丟進一個叫 `Default_Point` 的節點。 | **自動採用（一般建議×3，2026-08-27）**：採 B；B＋報告，或 C |
| R2O-ED-12 | Camera 多視埠／具名視圖真相 | 哪個 viewport | **共識 強烈建議×2**（G一／C強／X強）。Rhino 端取 `doc.Views.ActiveView.ActiveViewport`（`LiveLink_R2O_Camera.py:51`…`getThinLensCamera()` 取 `cams[1]`（`LiveLink_R2O_Camera.lua:49-50`），場景有多台 Thin Lens 相…；非透視視埠停止；Octane 端零台或多台且未指定時也停止，不得以 `cams[1]` 猜目標。 | **自動採用（強烈建議×2，2026-08-27）**：Rhino 端取 `doc.Views.ActiveView.ActiveViewport`（`LiveLink_R2O_Camera.py:51`）**… |
| R2O-ED-13 | Camera 持續發布 | A 維持 event＋throttle；B 僅手動 | **共識 一般建議×3**（G一／C一／X一）。方向：採 A。A；`LiveLink_R2O_Camera.py:100-101` 在距離上次寫檔不足 0.2 秒時直接 `return`，沒有補送最後一次。快速轉動視角後放手，最後一個位置很可能永遠不會寫出去，Octane 就停在中途的畫面上——重跑一次才會對齊。 | **採用 A（2026-08-27）**：Rhino 維持 event＋throttle，並補 trailing write／手動推送。Octane 端消費方式見 **R2O-ED-19**（與本列分開）。 |
| R2O-ED-14 | Camera lifecycle 不穩時是否允許 C# Gate | A 允許有證據後開；B 禁止 C# | **共識 輕鬆建議×2**（G一／C輕／X輕）。方向：採 A。目前沒有任何 Camera lifecycle 的實測紀錄，`RhinoView.Modified` 也還沒有已知問題。若門檻只寫「不穩時」，永遠不會有人能判斷條件是否成立。；只有同機可重現、排除重複註冊與 debounce 問題，且 Python 修正仍達不到明定穩定性／延遲門檻時，才建立最小 C# spike。 | **自動採用（輕鬆建議×2，2026-08-27）**：採 A；目前沒有任何 Camera lifecycle 的實測紀錄，`RhinoView.Modified` 也還沒有已知問題 |
| R2O-ED-15 | 燈光 vs 家具代理 | A 皆為 Point 子類型；B 燈光另指令（勿硬抄 R2B Light） | **共識 一般建議×3**（G一／C一／X一）。方向：採 A。A，用 type 欄區分；依據：現況 Point 通道對燈與家具完全不區分，兩者都只是「某個圖層底下的點或 Block」（`LiveLink_R2O_Point.py:67-91`）；程式說明舉的 `R2O::LT_Points::Downlight_A` 與 `R2O::FUR_Points::Sofa_B` 只是命名慣例，沒有任何程式含義。 | **自動採用（一般建議×3，2026-08-27）**：採 A；A，用 type 欄區分 |
| R2O-ED-16 | 燈能量／顏色／IES | A 只同步位置；B 從 Rhino 同步；C Octane 端自管 | **共識 強烈建議×3**（G強／C強／X強）。方向：採 A。初版 A 或 C，避免半套參數；這題其實沒有選擇空間：Rhino 端讀的是 Point 物件，幾何點本來就沒有能量、顏色或 IES 可讀。要同步這些就得先在 Rhino 建一套自訂資料（UserText），那既是新功能，也會寫進來源文件、違反 XF-ECO-03。 | **自動採用（強烈建議×3，2026-08-27）**：採 A；初版 A 或 C，避免半套參數 |
| R2O-ED-17 | Shortcut 預設安裝範圍 | A 全裝；B LiveLink 必裝、Authoring 選裝 | **共識 輕鬆建議×2**（G一／C輕／X輕）。方向：採 B。現況 `R2O_Shortcuts.txt`：Camera 是 `Ctrl+Q`，Authoring 四支都有鍵（`Alt+A`、`Shift+M`、`Ctrl+T`、`Ctrl+Shift+T`），只有 Point 沒有指定快捷鍵。；LiveLink 指令必裝，Authoring 快捷鍵選裝；自訂快捷鍵應保存在獨立使用者設定並在升級後重套，不再以改寫 `.lua` 原始碼作唯一儲存方式。Point 補一個不衝突的預設鍵前先掃描現有綁定。 | **自動採用（輕鬆建議×2，2026-08-27）**：採 B；現況 `R2O_Shortcuts.txt`：Camera 是 `Ctrl+Q`，Authoring 四支都有鍵（`Alt+A`、`Shift+M`、`C… |
| R2O-ED-18 | Python↔Lua 資料檔格式 | A 維持 Lua 資料檔；B 改 JSON（Lua 仍 apply）；C 其他 | **無同強度兩票**（G一／C？／X強）。先 A＋嚴格 schema；大改成本高；不同意 Grok，建議 B（JSON）。這是兩個 AI 唯一明確分歧的一列。 | **採用 B（2026-08-27）**：改 JSON；Lua 只 parse／apply，禁止對專案資料 loadfile() |
| R2O-ED-19 | Octane Camera 如何消費 Rhino 已發布的相機檔 | A 維持僅手動一次（Ctrl+Q／跑 Lua）；B 2.0 再試可開關的 real-time 輪詢（關＝回到手動一次）；C 強制永遠輪詢、不可關 | **無同強度兩票**（G？／C？／X？）。—（使用者 2026-08-27 補列現況與目標）；—（同上） | **採用 B（2026-08-27）**：**1.x 現況**：Rhino 持續寫檔（event＋約 0.2s 節流）；Octane **不**背景讀檔，按 Ctrl+Q（或跑一次 `LiveLink_R2O_Camera.lua`）才套用最新值——早期若持續讀檔曾有 lag，故改成「執行一次指令再同步」。**2.0 目標**：再試 Octane 端 timer／mtime 輪詢 real-time；必須可關回手動一次；讀 **JSON**（ED-18）；僅 revision／mtime 變才 parse＋apply；搭配 ED-13 trailing write 與 atomic；並評估 Dropbox 下 `live/` 高頻讀寫。lag 復發則預設改回手動，不刪 real-time 開關。 |

---

## D. R2O 細項（命名／schema／節點）

| ID | 要決定什麼 | 選項 | 整合建議（Grok／Claude／Codex） | 你的決定／說明 |
| --- | --- | --- | --- | --- |
| R2O-ND-01 | command ID | A 沿用 `R2O_Models`／`R2O_Scatter`／`R2O_Point`／`R2O_Camera`／`R2O_Open`；B 改名 | **共識 輕鬆建議×2**（G一／C輕／X輕）。方向：採 A。`rhino指令.md` 已用這 5 個名稱建好按鈕與巨集，改名只會多一次雙機同步成本，沒有換得任何東西。；指令名維持，降低按鈕、快捷鍵與文件變動；版本隔離放在 module／安裝目錄與 schema，不靠改使用者入口。 | **自動採用（輕鬆建議×2，2026-08-27）**：採 A；`rhino指令.md` 已用這 5 個名稱建好按鈕與巨集，改名只會多一次雙機同步成本，沒有換得任何東西 |
| R2O-ND-02 | 預設輸出檔名 | A 維持 `R2O.usdz`、`R2O_Camera_Sync_Data.lua`、`R2O_Point_Sync_Data.lua`；B 改小寫角色名 | **共識 一般建議×3**（G一／C一／X一）。方向：採 A。A 或小改進 config 資料夾後的相對名；現況三個檔名各有各的風格；既然 XF-ECO-02 已經把它們放進 `loopflow_R2O/` 專屬資料夾，檔名裡的 `R2O_` 前綴已經沒有作用了。建議 `models.usdz`／`camera.<ext>`／`point.<ext>`，各自再配一個 `*_pending`。 | **自動採用（一般建議×3，2026-08-27）**：採 A；A 或小改進 config 資料夾後的相對名 |
| R2O-ND-03 | 設定檔 | A `R2O_Path.txt`；B JSON | **共識 一般建議×2**（G一／C一／X強）。方向：採 B。B＋讀舊 txt migration；現行 parser 用 `line.split(":", 1)` 取鍵值，`if key in config` 之外的鍵靜默忽略，值也完全不驗證（`LiveLink_R2O__Config.py:171-176`）。 | **自動採用（一般建議×2，2026-08-27）**：採 B；B＋讀舊 txt migration |
| R2O-ND-04 | schema_version | A Camera＋Point 共用；B 分通道 | **共識 一般建議×3**（G一／C一／X一）。方向：採 B。B，較不易互相卡住；依據：兩個通道的變更節奏本來就不同——Camera 是事件驅動、每秒可能寫好幾次；Point 是手動整批發布。綁在同一個版本號上，改 Point 格式就會讓所有正在跑的 Camera 同步一起失效。 | **自動採用（一般建議×3，2026-08-27）**：採 B；B，較不易互相卡住 |
| R2O-ND-05 | 字串跳脫（引號／反斜線／換行／CJK／emoji／空名） | A 硬失敗；B 消毒後寫入 | **共識 強烈建議×3**（G強／C強／X強）。契約寫死；預設 A 對結構欄、顯示名可 B；(1) Point 完全沒有做任何處理，圖層名原樣寫進 Lua 字串（`LiveLink_R2O_Point.py:93`）。 | **自動採用（強烈建議×3，2026-08-27）**：契約寫死 |
| R2O-ND-06 | `PointNgName`／`PointPrefix` | A 維持 `R2O_Point`／`R2O_Point_`；B 改 | **共識 輕鬆建議×3**（G輕／C輕／X輕）。方向：採 A。A；這兩個值決定 Octane 場景裡既有節點找不找得到（`LiveLink_R2O_Point.lua:137、150`）；一改名，你現有場景裡所有節點會同時變成孤兒、而且新節點會另外長一批。 | **自動採用（輕鬆建議×3，2026-08-27）**：採 A；A |
| R2O-ND-07 | 哪些 `NT_*`／`P_*`／pin 是硬契約 | 列出凍結清單 vs 全當實作細節 | **共識 強烈建議×3**（G強／C強／X強）。對外行為當契約；內部 pin 盡量少暴露；我把 8 支 Lua 全數盤過，LiveLink 主鏈只用到 8 個符號：Point 用 `NT_GEO_SCATTER`、`A_TRANSFORMS`、`GT_STANDARD`； | **自動採用（強烈建議×3，2026-08-27）**：對外行為當契約 |
| R2O-ND-08 | `_LoopFlow_Config/loopflow_R2O/` 確切檔清單 | config／camera／point／pending／last-good／log／usdz 位置 | **共識 一般建議×2**（G一／C一／X強）。即時 Lua／USDZ last-good 都在該資料夾或子資料夾，不用 AppData；(1) 先解 `XF-ED-04`，否則清單訂了 Octane 也讀不到。(2) `cursor_R2O_debug_log.txt` 這個檔名帶著開發工具名（`LiveLink_R2O__Config.py:42`），2.0 建議改成 `r2o.log` 並一起搬進來。 | **自動採用（一般建議×2，2026-08-27）**：即時 Lua／USDZ last-good 都在該資料夾或子資料夾，不用 AppData |
| R2O-ND-09 | 是否仍允許 `ModelDir` 覆寫 | A 允許；B 一律工作檔旁 | **共識 一般建議×3**（G一／C一／X一）。方向：採 B。B，與 LoopFlow／路徑定案一致；補一個現況事實：`ModelDir` 留空時 fallback 到 `DataPath`＝AppData（`LiveLink_R2O_Models.py:179`…`LiveLink_R2B_Models.py:68`）。 | **自動採用（一般建議×3，2026-08-27）**：採 B；B，與 LoopFlow／路徑定案一致 |
| R2O-ND-10 | 未存檔 | A 硬停（現況）；B 暫存後備 | **共識 強烈建議×3**（G強／C強／X強）。方向：採 A。A；Models 對已存檔文件會直接 `_-Save`（`LiveLink_R2O_Models.py:184`）；對未存檔文件則因為 `sc.doc.Path` 為空而跳過存檔、繼續匯出到 AppData。 | **自動採用（強烈建議×3，2026-08-27）**：採 A；A |
| R2O-ND-11 | Shortcut ID 2.0 | A 凍結 1.x；B 可破壞＋migrator | **共識 一般建議×3**（G一／C一／X一）。方向：採 A。能不動就 A；快捷鍵不是存在設定檔裡，而是由 `__Setup_Shortcuts.lua` 直接改寫每一支 `.lua` 原始碼裡的 `-- @shortcut` 那一行（`:134、:141`…`R2O_Shortcuts.txt`、覆蓋檔案後再重跑一次 setup」，否則每次升級都要重設快捷鍵。 | **自動採用（一般建議×3，2026-08-27）**：採 A；能不動就 A |
| R2O-ND-12 | 座標／FOV（35mm）數學 | A 凍結 1.x＋fixtures；B 重定 | **共識 強烈建議×3**（G強／C強／X強）。方向：採 A。A；現行數學是：座標 `(x, z, -y)` 轉成 Y-up、長度乘上 `UnitScale(模型單位 → 公尺)`、FOV 用 `2·atan(36 / (2·鏡頭mm))`…`LiveLink_R2O_Camera.py:72-74`）、Block 的矩陣轉換（`LiveLink_R2O_Point.py:87-8… | **自動採用（強烈建議×3，2026-08-27）**：採 A；A |

---

## E. 已確認、不在本表重複決定

- 目標 `v2.0.0`；不移動 `v1.0.0`。
- 整合 `v2-development`；短期 `codex/v2-<scope>`。
- 執行時父路徑：`_LoopFlow_Config/loopflow_R2O/`（2026-08-26）。
- 測檔環境變數：`LOOPFLOW_R2O_WORKFILES_ROOT`。
- 來源零變更；last-good；Authoring 本輪不擴功能。
- 不與 R2B 共用 runtime。
- Models 消費：手動置換 USDZ（ED-02＝A）。
- Point 身分：`type_id` 不寫回 Rhino（ED-05）；刪節點＝A（ED-10）。
- Camera：Rhino 持續發布（ED-13＝A）；Octane 2.0 再試可開關 real-time 輪詢（ED-19＝B）；資料 JSON（ED-18＝B）。
- 開發按鈕與指令清單 SSOT：`wip/docs/rhino指令.md`。

---

## F. 回寫檢查清單（確認後由 AI 執行）

- [ ] `資料契約.md`
- [ ] `系統設定.md`（入口改指向 `rhino指令.md`）
- [ ] `工作流程.md`
- [ ] `開發任務與路徑.md`／`PROGRESS.md`
- [x] 跨產品 XF 與 R2B 決策表對齊（含新增的 `XF-ED-04`）
- [x] 重新產生 `資料生態決策表_三家建議.html`

---

## G. 下一步（使用者）

多數列已自動採用；`R2O-ED-02`＝A（手動置換）、`R2O-ED-05`／`10` 已依合併模擬讀法定案、`R2O-ED-18`＝B、`R2O-ED-19`＝B；建議操作序 Models→Camera→Point→Scatter。開發順序：**先 R2B＋Blender，再 R2O＋Octane**。請覆核；特別看：


1. `XF-ED-04`：Octane 如何找到搬家後的設定。
2. `R2O-ED-19`：Camera real-time 輪詢的 lag／可關回手動。
3. `R2O-ED-05`：牽動 ED-06、ND-05、ND-06。
4. `R2O-ECO-03`：現況會回報不實成功。
